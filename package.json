{
  "name": "bce-erc-20-and-dex",
  "version": "1.0.0",
  "description": "ERC-20 and DEX project from BlockchainExpert. Deploys an ERC-20 token and is able to be interacted with from another smart contract",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.3"
  }
}
